apiVersion: apps/v1
kind: Deployment
metadata:
  name: execution-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: execution-service
  template:
    metadata:
      labels:
        app: execution-service
    spec:
      automountServiceAccountToken: false
      containers:
        - name: execution-service
          image: ghcr.io/your-org/execution-service:v1.0.0
          ports:
            - containerPort: 3003
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
              ephemeral-storage: "256Mi"
            limits:
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: execution-service
spec:
  selector:
    app: execution-service
  ports:
    - port: 80
      targetPort: 3003